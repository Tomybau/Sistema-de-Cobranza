# Frontend

This directory will contain the landing page and web assets for this project.

## Setup

When you're ready to build, run `/landing-builder` (or just ask Claude to build the landing page).
Claude will use the `landing-builder` global skill, which starts with 5 intake questions before generating any code.

The landing page will be scaffolded here inside `frontend/`.
